## What does this PR do?

Closes #

## How was it tested?

- [ ] `cargo test` passes
- [ ] `cargo fmt --all --check` and `cargo clippy --all-targets -- -D warnings` pass
- [ ] New behaviour has tests (contracts) or was run against testnet (keeper-bot)
- [ ] Docs updated if the public interface changed (functions, events, error codes)
